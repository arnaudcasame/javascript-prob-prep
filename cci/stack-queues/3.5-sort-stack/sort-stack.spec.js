import {sortStack} from './sort-stack.js';

export default describe('#Sort Stack', function() {
    xit('title', function() {
        // eslint-disable-next-line no-undef
        expect(sortStack()).to.equal();
    });
});
