import {threeInOne} from './three-in-one';

export default describe('title', function() {
    xit('title', function() {
        // eslint-disable-next-line no-undef
        expect(threeInOne()).to.equal();
    });
});
