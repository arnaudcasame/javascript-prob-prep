import {queueViaStacks} from './queue-via-stacks.js';

export default describe('#Queue Via Stacks', function() {
    xit('title', function() {
        // eslint-disable-next-line no-undef
        expect(queueViaStacks()).to.equal();
    });
});
