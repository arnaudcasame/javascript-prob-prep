import {animalShelter} from './animal-shelter.js';

export default describe('#Animal Shelter', function() {
    xit('title', function() {
        // eslint-disable-next-line no-undef
        expect(animalShelter()).to.equal();
    });
});
