import {stackOfPlates} from './stack-of-plates.js';

export default describe('#Stack Of Plates', function() {
    xit('title', function() {
        // eslint-disable-next-line no-undef
        expect(stackOfPlates()).to.equal();
    });
});
