import { loopDetection } from "./loop-detection.js";

export default describe('Loop Detection (Corrupt LinkedList)', function(){
    xit('title', function(){
        expect(loopDetection([])).toEqual(0);
    });
});