import { intersect  } from "./intersection.js";

export default describe('Intersection of 2 LinkedLists', function(){
    xit('title', function(){
        expect(intersect([], [])).to.equal(0);
    });
});