import {listOfDepths} from './list-of-depths.js';
import {expect} from 'chai';

export default describe('#4.3: List of Depths', function() {
    xit('Should return ', function() {
        expect(listOfDepths()).to.equal([]);
    });
});
