/**
 * Checks if a binary tree is a binary search tree.
 * @param {object} graph
 * @param {string} root
 * @return {boolean}
 */
export const validateBST = function(graph, root) {
    return false;
};

