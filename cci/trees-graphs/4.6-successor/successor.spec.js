import {isSuccessor} from './successor.js';
import {expect} from 'chai';

export default describe('#4.6: Successor', function() {
    xit('Should return', function() {
        expect(isSuccessor).to.equal(null);
    });
});
