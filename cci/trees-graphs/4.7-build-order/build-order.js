/**
 * Finds a build order that will allow the projects to be built.
 * @param {string[]} projects List of projects
 * @param {string[][]} dependencies
 * @return {string[]} List of built projects
 */
export const buildOrder = function(projects, dependencies) {
    return [];
};
