/**
 * Given two strings, sl and s2, write code to check
 * if s2 is a rotation of sl using only one call to
 * isSubstring (e.g.,"waterbottle" is a rotation of"erbottlewat").
 * @param {string} s1
 * @param {string} s2
 * @return {boolean}
 */
export const isSubstring = function(s1, s2) {
    return false;
};
