/**
 * Check if they are one edit
 * (or zero edits) away from each other.
 * @param {string} s1
 * @param {string} s2
 * @return {boolean}
 */
export const oneAway = function(s1, s2) {
    return false;
};
